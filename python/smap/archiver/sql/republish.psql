-- -*- sql -*-
-- sql table definition for the postgres republish client
CREATE TABLE republish (
       inserted_at DATETIME NOT NULL DEFAULT CURRENT_DATE,
       key VARCHAR(36),
       obj TEXT,
       sent BOOLEAN DEFAULT FALSE
);
